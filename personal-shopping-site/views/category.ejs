<!DOCTYPE html>
<html lang="en">
<head>
    <title>Categories | E-Commerce</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <h1>Products</h1>
        <a href="/">Home</a>
    </header>
    <div class="products">
        <% products.forEach(product => { %>
            <div class="product-card">
                <img src="<%= product.isBundle ? product.images[0] : product.image %>" alt="<%= product.name %>">
                <h2><%= product.name %></h2>
                <p>Category: <%= product.category %></p>
                <p>Price: ₹<%= product.price %></p>
                <a href="/products/<%= product.id %>">View Product</a>
                <form action="/products/buy" method="POST" style="margin-top: 10px;">
                    <input type="hidden" name="productId" value="<%= product.id %>">
                    <button type="submit">Buy Now</button>
                </form>
            </div>
        <% }); %>
    </div>
</body>
</html>
